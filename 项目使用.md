# 1. git脚本使用

第一次配置远程并推送：

```
bash scripts/push_repo.sh --remote-url git@github.com:你的用户名/你的仓库.git 
```

之后通常直接一条命令：

```
bash scripts/push_repo.sh 
```

如果想自己写提交信息：

```
bash scripts/push_repo.sh --message "chore: sync latest progress" 
```

如果你已经手动 commit 过，只想推送：

```
bash scripts/push_repo.sh --skip-commit 
```

如果想先看看它会执行什么：

```
bash scripts/push_repo.sh --dry-run --remote-url git@github.com:你的用户名/你的仓库.git
```

# 2.场景浏览器使用

运行方式不变：

```bash
source /home/zyc/miniconda3/etc/profile.d/conda.sh
conda activate opening_preexp
python scripts/browse_scene.py 
```

常用按键：

- W/A/D 移动和转向
- M 切换 RGB / depth / semantic
- N / P 切题
- H 隐藏/显示 HUD
- C 截图
- Q 退出